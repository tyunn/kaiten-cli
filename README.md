# Kaiten CLI

CLI-инструмент для работы с Kaiten API.

## Установка

### 1. Клонируйте репозиторий
```bash
git clone <repo-url> kaiten-cli
cd kaiten-cli
```

### 2. Настройка переменных окружения
```bash
cp .env.example .env
```

Откройте `.env` и укажите ваши данные:

```env
KAITEN_API_URL=https://ваш-домен.kaiten.ru/api/latest
KAITEN_API_TOKEN=ваш_api_токен
KAITEN_DEFAULT_SPACE_ID=12345
```

**Как получить данные:**
- **API URL**: это адрес вашего пространства Kaiten (например: https://company.kaiten.ru/api/latest)
- **API Token**: зайдите в настройки профиля в Kaiten → "API токены" → создайте новый токен
- **Space ID**: можно узнать из URL при открытии нужного пространства (число после `/space/`)

### 3. Установка зависимостей
```bash
npm install
```

### 4. Глобальная установка (опционально)
```bash
npm install -g .
```

Теперь можно использовать команду `kaiten` из любого места.

## 📋 Использование

### Без глобальной установки
```bash
npm start simple
npm start card-simple 45791547
npm start cards
npm start card 45791547
npm start help
```

### После глобальной установки
```bash
kaiten simple
kaiten card-simple 45791547
kaiten cards
kaiten card 45791547
kaiten help
```

## 🎯 Оптимизация

- Детальная карточка: 91% меньше данных
- Список карточек: 96% меньше данных

### Что оптимизировано:
- Удалены base64 аватары
- Убраны избыточные метаданные
- Оптимизированы форматы дат и времени
- Сокращены описания до 500 символов
- Только важная бизнес-информация

## 🔧 Доступные команды

| Команда | Описание |
|---------|----------|
| `simple` | Список задач (человекочитаемый) |
| `card-simple <id>` | Детали задачи (человекочитаемый) |
| `cards` | Список задач (JSON) |
| `card <id>` | Детали задачи (JSON) |
| `help` | Справка |

## 💡 Примеры

```bash
# Показать все активные задачи
kaiten simple | grep "✅"

# Получить детали задачи
kaiten card-simple 45791547

# Фильтровать задачи по колонке (требуется jq)
kaiten cards | jq '.[] | select(.column == "В работе")'
```

## 📊 Результаты оптимизации

| Тип данных | Оригинал | Оптимизировано | Экономия |
|-----------|----------|----------------|----------|
| Детали задачи | 10,051 символов | 878 символов | 91% |
| Список задач | 135,675 символов | 5,909 символов | 96% |
