# ============================================
# БИЗНЕС ЛОГИКА ПРОЕКТА
# ============================================

# Этот файл содержит параметры конкретного проекта:
# - Пространство (Space) где находится проект
# - Доска (Board) где ведётся разработка
#
# Эти параметры определяют бизнес логику вашего проекта,
# поэтому файл можно безопасно коммитить в git.

# Пространство по умолчанию
# Все операции с карточками будут использовать это пространство
KAITEN_DEFAULT_SPACE_ID=12345

# Доска по умолчанию
# Все операции создания карточек будут использовать эту доску
KAITEN_DEFAULT_BOARD_ID=67890

# ============================================
# ОГРАНИЧЕНИЕ ДОСТУПА (ОПЦИОНАЛЬНО)
# ============================================

# Список разрешённых пространств (через запятую)
# Если указан, SDK будет блокировать операции с пространствами вне этого списка
# Полезно для команд которые работают с несколькими проектами
# Пример: 12345,67890
# KAITEN_ALLOWED_SPACE_IDS=12345,67890

# Список разрешённых досок (через запятую)
# Если указан, SDK будет блокировать операции с досками вне этого списка
# Полезно для ограничения доступа к конкретным доскам
# Пример: 111,222,333
# KAITEN_ALLOWED_BOARD_IDS=111,222,333

# ============================================
# ПОЯСНЕНИЯ
# ============================================

# 1. Настройки доступа (API_URL, API_TOKEN) должны быть
#    в файле ~/.kaiten/config (глобальные настройки)
#
# 2. Этот файл (.kaiten.env) можно коммитить в git
#    так как он содержит только бизнес логику (ID досок/пространств)
#
# 3. Изменение токена доступа не требует коммита в этот файл
#    токен хранится только в ~/.kaiten/config
#
# 4. Копируйте этот файл в .kaiten.env для использования
#    cp .kaiten.config.example .kaiten.env
