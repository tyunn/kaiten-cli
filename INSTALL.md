# 🎉 Установка завершена!

## 📦 Проект Kaiten CLI создан

**Расположение:** `/home/anton/projects/github.com/tyunn/kaiten-cli`

## 🚀 Использование из любого каталога

Команда `kaiten` теперь доступна глобально! Вы можете использовать её из любого места:

```bash
# Из любого каталога
cd /any/where/in/your/system
kaiten simple                          # Список задач
kaiten card-simple 45791547           # Детали задачи
kaiten cards                          # JSON список
kaiten card 45791547                  # JSON детали
kaiten help                           # Справка
```

## 📁 Локальное использование

Если нужно работать с конкретной версией проекта:

```bash
cd /home/anton/projects/github.com/tyunn/kaiten-cli
npm start simple
npm start card-simple 45791547
```

## ⚙️ Конфигурация

Файл `.env` находится в: `/home/anton/projects/github.com/tyunn/kaiten-cli/.env`

Если нужно изменить настройки:
```bash
cd /home/anton/projects/github.com/tyunn/kaiten-cli
nano .env
```

## 🔧 Обновление

Чтобы обновить CLI до последней версии:

```bash
cd /home/anton/projects/github.com/tyunn/kaiten-cli
npm install -g .
```

## 📊 Результаты оптимизации

| Тип данных | Оригинал | Оптимизировано | Экономия |
|-----------|----------|----------------|----------|
| Детали задачи | 10,051 символов | 878 символов | **91%** |
| Список задач | 135,675 символов | 5,909 символов | **96%** |

## 💡 Примеры использования

### Просмотр задач при программировании:
```bash
# В вашем проекте
cd /home/anton/projects/github.com/tyunn/your-project
kaiten simple | grep "В работе"
```

### Получение деталей текущей задачи:
```bash
kaiten card-simple 45791547
```

### Поиск по ключевым словам:
```bash
kaiten simple | grep -i "шахмат"
```

### Интеграция с другими инструментами:
```bash
# Сохранить список в файл
kaiten cards > tasks.json

# Обработать с jq
kaiten cards | jq '.[] | select(.status == "Активна")'
```

## 🛠️ Доступные команды

| Команда | Описание | Формат |
|---------|----------|--------|
| `simple` | Список задач | Человекочитаемый |
| `card-simple <id>` | Детали задачи | Человекочитаемый |
| `cards` | Список задач | JSON |
| `card <id>` | Детали задачи | JSON |
| `help` | Справка | - |

## ✅ Всё готово к использованию!

Теперь вы можете работать с Kaiten из любого каталога вашей системы!
